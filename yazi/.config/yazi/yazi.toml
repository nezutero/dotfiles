[manager]
show_hidden = true

[opener]
pdff = [
    { run = 'zathura "$@"', desc = "zathura", block = true, for = "unix" },
]

[open]
rules = [
    {mime = "application/pdf", use = [ "pdff", "reveal" ] },
]
